version: "3"
containers:
- name: vault-server
  image: hashicorp/vault:1.15.4
  cmd: version
  shell: /bin/sh -l
  ports:
  - 8200
  environment:
    SKIP_SETCAP: "true"
    VAULT_ADDR: http://localhost:8200
    VAULT_TOKEN: root
  memory: 128
