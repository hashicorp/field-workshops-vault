version: "2"
containers:
- name: vault-cli
  image: vault.1.2.3
  shell: /bin/bash
  ports:
  - 8200
  environment:
    VAULT_DEV_ROOT_TOKEN_ID: root
    VAULT_ADDR: http://localhost:8200
    VAULT_TOKEN: root
